type Props = {
  fromTo: string;
  role: string;
  position: string;
  content: string;
  tools: string[];
};
const ExperienceList = (props: Props) => {
  return <div></div>;
};

export default ExperienceList;
